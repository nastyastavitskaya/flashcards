<%= simple_form_for @category do |f| %>
  <%= f.input :name, label: t('category.name'), placeholder: t('category.name_placeholder') %>

 <%= f.button :submit, t('category.submit'), class: 'btn btn-success' %>
<% end %>

