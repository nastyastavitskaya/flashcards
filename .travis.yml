language: ruby
sudo: required
rvm:
  - 2.0.0
  - 2.1.1
  - 2.2.2
  env:
  - DB=postgresql
script:
  - bundle exec rspec spec
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database strano_db;' -U postgres
